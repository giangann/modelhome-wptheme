import { Box, Typography } from '@mui/material';

import { PuppinsTypo } from '../styled';
import { Footer } from './Footer';

export const Layout = () => {
  return (
    <Box>
      <Footer />
    </Box>
  );
};
